[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: serve index.html for every route so React Router
# handles deep links like /pricing on direct load or refresh
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
